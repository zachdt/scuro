name: Verify

on:
  push:
    branches:
      - main
  pull_request:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.11.1

      - uses: oven-sh/setup-bun@v2

      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.7

      - name: Install shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Bun checks
        run: bun run --cwd ops/testnet check

      - name: Bun tests
        run: bun test --cwd ops/testnet

      - name: Targeted Forge build
        run: |
          forge build \
            script/testnet/TestnetDeployCommon.s.sol \
            script/testnet/DeployCore.s.sol \
            script/testnet/DeployNumberPickerModule.s.sol \
            script/testnet/DeploySlotModule.s.sol \
            script/testnet/DeployFinalize.s.sol \
            script/testnet/SmokeNumberPicker.s.sol \
            script/testnet/SmokeSlot.s.sol

      - name: Focused Forge tests
        run: |
          forge clean
          forge test --match-path 'test/{ProtocolCore,NumberPickerAdapter,SlotMachineController}.t.sol' --offline -vv
          forge test --match-path 'test/invariants/*.t.sol' --offline -vv

      - name: Terraform fmt
        run: terraform -chdir=infra/hetzner-cloudflare/testnet fmt -check -diff

      - name: Terraform validate
        run: |
          terraform -chdir=infra/hetzner-cloudflare/testnet init -backend=false
          terraform -chdir=infra/hetzner-cloudflare/testnet validate

      - name: Shellcheck
        run: shellcheck -x script/testnet/*.sh ops/testnet/runtime/*.sh

      - name: Build Linux bundle
        run: SCURO_BUNDLE_INCLUDE_HOST_TOOLS=0 bash script/testnet/build_bundle.sh /tmp/scuro-testnet-bundle.tar.gz
